// Copias de rv y datos del paciente
const rv = {returned_values};
const patientName = "{patient_name}";
const patientLastName = "{patient_last_name}";
const patientDOB = "{patient_dob}";

const memberInfo = document.getElementById('memInfo');
if(memberInfo) {
    
}
